{
    "name": "rclone",
    "description": "Deploy rclone on Heroku.",
    "keywords": ["rclone"],
    "env": {
        "rclone": {
            "description": "$HOME/.config/rclone/rclone.conf",
            "value": "",
            "required": true
        },
        "parameter": {
            "description": "The following is just an example,Please modify it yourself",
            "value": "gd:up --transfers 5 --vfs-cache-mode full  --user (Login_user) --pass (Login_password)",
            "required": true
        }
    },
    "stack": "container"
}
